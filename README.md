demo.py should be run from the directory above "pycha"


you'll want to:

> pip install cffi cairocffi


> apt-get install libffi-dev libcairo-dev

----

"a reasonably-fast non-interactive friendly-to-hack backend plotting library"
